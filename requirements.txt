# Pinned runtime + dev dependencies for mech_security.
# Generated 2026-05-30 from a known-working install (Python 3.11.10, macOS MPS).
# Authoritative dep list lives in pyproject.toml; this file pins versions
# for reproducible installs (`pip install -r requirements.txt`).
#
# To regenerate after a deliberate upgrade:
#   pip install --upgrade <pkg>  # then update the pin below
# Don't `pip freeze > requirements.txt` blindly — it picks up transitive
# deps + system packages that don't belong here.

# Core ML
torch==2.6.0
transformers==5.9.0
transformer-lens==3.2.1

# Numerical / data
numpy==1.26.4
scikit-learn==1.6.1
jaxtyping==0.3.9

# Utilities
tqdm==4.67.1
python-dotenv==1.0.1
pandas>=2.0.0

# Datasets (HarmBench / Alpaca loaders use it)
datasets>=2.14.0

# API client (Claude judge)
anthropic==0.45.2

# Plotting (experiment runners write figures)
matplotlib==3.10.9

# Dev / test
pytest==8.3.4
pytest-asyncio==1.3.0
ruff==0.14.6
